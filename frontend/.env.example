# Supabase public (safe to commit — these are public keys)
# Found in Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SITE_URL=http://localhost:5173

# NOTE: Never put service-role/secret keys in frontend env vars.
# The anon/publishable key is safe for public use because RLS protects data.
